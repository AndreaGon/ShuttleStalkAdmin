export interface Route{
    id?: string,
    routeName: string,
    driver: string[],
    shuttle: string[],
    pickupTime: string[],
    dropoffTime: string[],
    route: string[],
    routeImage: string
}